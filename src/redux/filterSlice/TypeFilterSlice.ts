export type TypeFilterValues = {
	valueBrand: string[];
	valueModel: string[];
	valueSizes: number[];
	valueColor: string[];
};

export type TypeFilterSliceSlice = {
	filterValues: TypeFilterValues;
};
